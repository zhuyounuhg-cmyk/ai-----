# 05 · 接手规划（红队复现任务）

> **定性**：本项目为**授权红队演练**（内部验证环境、复现与加固研究），非真实攻击行为。
> 目标读者：后续接手工程师 / AI 助手。
> 日期：2026-09-17 · 仓库：`D:\ai攻击测试包` · 分支/目录：`gpt再改`

---

## 一、任务是什么（一句话）

复现并修复「**假认证 → 进入游戏 → 辅助窗口显示**」链路，产出可交接的工程与文档，并据此做**针对性加固**（红队复现 → 蓝队加固）。

## 二、现状（截至 2026-09-17 夜）

| 环节 | 状态 |
|---|---|
| 假认证（外层登录绕过） | ✅ 完成（有正/负对照） |
| 到达二级页 | ✅ |
| 二级认证链路（点①触发 `kmlogin&app=54010`） | ✅ 走通（四字段回放；判据待定，非阻塞） |
| ② 检查链（分身大师 / 自签名 / meta / 游戏包） | ✅ 全过 |
| **游戏启动** | ✅ 实测拉起（进程 + ACE 反作弊初始化 + 联网） |
| **辅助窗口直调（OpenImgui）** | ⏳ 代码完成（同步直驱版已构建 `D67AF5A7…`），**待装机验证** |
| 游戏内显示辅助窗口 | ⏳ 待验证（依赖上项） |

## 三、距离复现还差的问题（按优先级）

1. **P1 辅助窗口**：装机（手机点「继续安装」）→ 跑 `scripts/phone-e2e-stage9.ps1` → 看 `DIRECT KICK imgui: OpenImgui()` / `SECONDARY OpenImgui ENTER`；若崩 → 查 logcat。
2. **P4 游戏运行深度**：目前黑屏初始化中；容器 + ACE 反虚拟化有风险——只需**如实记录"能到哪一步"**。
3. **P5 二级认证判据**：补对照实验（418 vs 200 回放变体；开关在 `pc_hook.js` 顶部）。
4. **P2 ②后重启循环**：已用「同步直驱」规避；机制待查（非阻塞）。
5. **P3 分身大师容器内 SIGSEGV**：记录在案（嵌套虚拟化边界）。

## 四、怎么继续改（执行规划）

- **阶段 1（约 1 小时）**：安装同步直驱版 APK → 跑 stage9 → 判定 P1（辅助窗口是否出现）。
- **阶段 2**：按结果微调 `pc_hook.js`（调用时机/顺序）→ 重建 APK → 重跑（重建命令见 04 号文档）。
- **阶段 3**：二级认证对照实验（只改回放值，不动其他逻辑）。
- **阶段 4**：整理「蓝队加固点」清单（示例：认证判据上移服务端、一次性凭据、防重打包已有、反插桩检测）。

## 五、文件与红线

- **要改（仅这三处）**：
  - `vendor/VirtualApp14/app/src/main/assets/pc_hook.js`（**唯一运行时行为点**）
  - `vendor/VirtualApp14/app/src/main/java/com/pc/PcGadget.java`
  - `scripts/phone-e2e-stage9.ps1`
- **不能改**：目标 APK 本体（Dex2C 校验）、游戏、分身大师、smali（只读参照）、`/sdcard/km|imei`（只兼容）、容器引擎其余部分。
- 完整清单与踩坑见《04-交接文档-给后续接手人.md》。

## 六、入口索引（在本测试包内）

| 内容 | 位置 |
|---|---|
| 工程快照 | `01-项目文件/phone-container-全量/` |
| 最新宿主 APK | `01-项目文件/宿主-最新构建产物/app-debug-同步直驱版-v2.1c.apk` |
| 问题清单 | `02-现状与问题/项目现状与未解决问题-20260917.md` |
| 实验记录 | `02-现状与问题/实验记录-二级认证与直驱-20260917.md` |
| 交接文档 | `04-交接文档-给后续接手人.md` |
| 本文件 | `05-接手规划-红队复现任务-20260917.md` |

---
*（本文件已同步提交至仓库分支 `gpt再改`。）*
